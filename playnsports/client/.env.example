# Client environment variables for development and production
# VITE_ prefix is required for Vite to expose the variable in code

# backend API base url (no trailing slash)
VITE_API_URL=http://localhost:5000/api

# socket server url
VITE_SOCKET_URL=http://localhost:5000
